environment: localhost
server:
  port: 9090
  servlet:
    context-path: /authsox
spring:
  kafka:
      bootstrap-servers: localhost:9092
  datasource:
    url: jdbc:postgresql://localhost:5433/AUTHS_SOX
    username: postgres
    password: postgres-pwd
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl: update
auth:
  tokens:
    issuer: athssox
    access-token-ttl-seconds: 3600
    refresh-token-ttl-seconds: 2592000
    email-verification-token-ttl-seconds: 86400
    jwt:
      alg: RS256
      key-id: ${AUTH_JWT_KEY_ID:local-dev}
      key-store:
        path: ${AUTH_JWT_KEY_STORE_PATH:}
        password: ${AUTH_JWT_KEY_STORE_PASSWORD:}
        alias: ${AUTH_JWT_KEY_STORE_ALIAS:}
        key-password: ${AUTH_JWT_KEY_STORE_KEY_PASSWORD:}
      pem:
        private-key-path: ${AUTH_JWT_PRIVATE_KEY_PATH:./keys/jwt-private.pem}
        public-key-path: ${AUTH_JWT_PUBLIC_KEY_PATH:./keys/jwt-public.pem}
        private-key: ${AUTH_JWT_PRIVATE_KEY:}
        public-key: ${AUTH_JWT_PUBLIC_KEY:}
    jwks:
      cache-seconds: 300
  session:
    last-used-throttle-minutes: 5
  cleanup:
    enabled: true
    cron: "0 0 0 * * *"
    zone: Europe/Kiev
security:
  email-verification:
    hmac-secret: ${SECURITY_EMAIL_VERIFICATION_HMAC_SECRET:}
  internal-auth:
    mode: dev-jwt
    service-name: ${SECURITY_INTERNAL_AUTH_SERVICE_NAME:authorisationservice-sox}
    dev:
      jwt-secret: ${SECURITY_INTERNAL_AUTH_DEV_JWT_SECRET:}
      issuer: ${SECURITY_INTERNAL_AUTH_DEV_ISSUER:sitionix-internal}
      accepted-audiences:
        - ${SECURITY_INTERNAL_AUTH_DEV_ACCEPTED_AUDIENCES:authorisationservice-sox}
      ttl-seconds: ${SECURITY_INTERNAL_AUTH_DEV_TTL_SECONDS:300}
    policies:
      notificationservice-sox:
        allow:
          - GET /api/v1/auth/emailVerificationTokens/*:issueLink
      bffservice-sox:
        allow:
          - "*"
outbox:
  worker:
    poll-delay-ms: 5000
    batch-size: 50
    retry-delay-seconds: 60
    max-retries: 5
